defaults:
  - 4k4d_prior_case_sparseproto_softmatte_selfgeom_r23_roi_bodyrehearsal
  - _self_

exp_name: 4k4d_prior_case_sparseproto_softmatte_selfgeom_r26_weakbody_nophoto_signed_pointbranch

# Local diagnostic only.
# r23 proved the ROI-masked body rehearsal case is wired correctly, but its
# pseudo depth/point targets dominate the point/depth heads in the gradient
# audit. This variant keeps signed point-branch self-geometry and the full-body
# bottom-line case, while demoting body rehearsal to a weak stabilizer.
# No hard teacher, no PnP camera replacement, and no photometric grid.

loss:
  human_prior:
    weight: 0.10
    depth:
      alpha: 0.0
      supervise_conf: false
      loss_weight: 0.04
    point:
      alpha: 0.0
      supervise_conf: false
      loss_weight: 0.06
