# V172 Current Route State

## Conclusion

V13050 remains downgraded. V137 Modal A10 training completed, but V138/V140 failed closed. V171 target assets were generated, and V172 adversarial training smoke ran locally, but it also failed closed.

This is not mentor-ready and not a true external hard block.

## What Changed

- V132 gate policy audit passed.
- V133 anti-billboard metric v2 was implemented.
- V134 billboard weak-region masks were generated for 4 cases.
- V135 anti-billboard topology-volume student was implemented with teacher/Kinect rejection.
- V136 loss smoke passed on a real 0012 sample.
- V137 Modal A10 training ran for 300 steps and wrote prediction PLY/NPZ outputs.
- V138 mentor visual boards were generated from V137 output.
- V140 causality gate failed closed because true still has billboard failures and controls remain close or stronger.
- V171 part-graph cross-section targets were generated for 4 cases.
- V172 adversarial smoke ran but did not solve the control-separation problem.

## Current Root Cause

The model can increase shell-like volume metrics, but it still does not produce stable, continuous, semantic 3D body volume. Same-topology and shuffled controls remain too competitive, so the route has not proven VGGT+SMPL semantic causality.

## Required Next

The next route should not simply extend the same loss. It should add a stronger topology-consistent decoder:

- multi-point shell decoding per anchor with budget-balanced resampling;
- part graph adjacency supervision using explicit edges;
- semantic adversarial controls in the batch rather than scalar post-hoc pressure;
- visual checkpoint selection using V138/V140 boards, not metrics alone.

Face detail remains not applicable; allowed claim stays limited to head/face contour and hair region.
