# Geometry Source Policy Acceleration Long-Process Plan (2026-03-28)

## Current State

- Guard remains `steady_hold`.
- Research loop is in `IDLE_GUARD`.
- There is no active `approved_problem.json`.
- There is no current priority family.
- There is no auto-next ticket.
- Cloud remains off.

## What Just Happened

- The first `source_policy_hybrid_ring_regularization / stablelead_nearest_plus_uniform_tail` ticket became a formal `provisional_lead` at `long_gate_100x20`.
- A fresh manual decision then `PROMOTE`d that result.
- The promoted local lead is now synced as the current local lead.

The previous five-family failure batch remains closed:

- `interpolated_eligibility_shaping`
- `partial_joint_depth_routing`
- `conf_reg_disagreement_routing`
- `unproject_consistency_routing`
- `unproject_aux_confgate`

## Explicit Planning Target

- `IDLE_GUARD`
- no active approval
- no current priority family
- no auto-next ticket
- promoted local lead synced
- cloud off
